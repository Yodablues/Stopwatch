// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "com.colarusso.stopwatch"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
